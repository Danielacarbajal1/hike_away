<%= simple_form_for([hike, favourite], remote: true) do |form| %>
<%= form.submit "Favourite", :type => :image, :src => current_user.favourites.find_by(hike_id: hike.id).nil? ? image_path('likee.svg') : image_path('like.svg'), :id => "heart_id", :class => "show-flaticon" %>
<% end %>
